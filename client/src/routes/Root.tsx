import * as React from 'react';
import {Switch, HashRouter as Router, Route} from 'react-router-dom';

const Root = () => {
	<Router>
		<Switch>
			<Route />
		</Switch>
	</Router>
};

export default Root;